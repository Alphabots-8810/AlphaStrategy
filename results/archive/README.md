Superseded outputs/scripts from intermediate runs (pre-audit-round-2 code). Kept for provenance only;
do not cite. Current results: ../validate_elite.md, ../alliance_{regular,champs}_B.md, ../sensitivity.md,
../tune_mcts_prune.md, ../tune_mcts_sweep.md, ../tie_rule_ab.md.

tune_mcts.log (here) came from an earlier version of experiments/tune_mcts.py without the prune
column; its greedy-net half was stopped midway. Its greedy-proc rows are reproduced digit for digit
by ../tune_mcts_sweep.md on the final code.
